{
  "name": "laxoria-backend",
  "version": "1.0.0",
  "description": "Laxoria backend - ready-to-deploy on Vercel (prebuilt dist included)",
  "main": "dist/index.js",
  "scripts": {
    "dev": "ts-node-dev index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "engines": {
    "node": "18.x"
  },
  "dependencies": {
    "express": "^4.18.2",
    "dotenv": "^16.0.3",
    "@prisma/client": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.2",
    "ts-node-dev": "^2.0.0",
    "prisma": "^5.0.0"
  }
}